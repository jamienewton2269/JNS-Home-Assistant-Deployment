# JNS Home Assistant Deployment Platform v4.2.3

Validation correction release based on GitHub Actions run 34150429413.

Changes:
- added `iot_class: local_push` to `manifest.json`;
- preserved Hassfest manifest order: `domain`, `name`, then alphabetical;
- moved/provided local brand assets at:
  `custom_components/jns_deployment/brand/icon.png`;
- added `custom_components/jns_deployment/brand/logo.png`;
- updated integration/internal version to 4.2.3;
- retained the root brand asset for compatibility with older HACS guidance.
